#!/bin/bash
# Copies the development version of the Database to the location where the program actually looks for it.
mkdir /Users/shiruba/.config
mkdir ~/.config/PlusAlpha
cp ./plusalpha.sqlite ~/.config/PlusAlpha/
